# Synthetic Enterprise Dataset

Phase 3 loads the 60 deterministic records in `customers.jsonl` for Criteria execution. Research applies every Hard constraint, orders matches by simple Soft preference weight, and caps the Phase 1-compatible response at five records. They are not real companies and must not be interpreted as external research results.
